interface:
  display_name: "Stack CLI"
  short_description: "Safe stacked-PR workflows for stack"
  default_prompt: "Use $stack-cli to guide a safe `stack` workflow with previews first, explicit parents, repairable restacks, and no GraphQL."

policy:
  allow_implicit_invocation: true
